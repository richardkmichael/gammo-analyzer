#!/usr/bin/env ruby

# FIXME:
#
# 1/ CLI provide a configuration instance, not use a Configuration singleton.
#    def configuration ; ; end
#
# 2/ Suppress summary output
#
# 3/ Request/suppress pry

require "gammo_analyzer"

cli = GammoAnalyzer::CLI.new

report_builder = GammoAnalyzer::ReportBuilder.new cli

puts report_builder.summary

cli.console if cli.interactive
